return {
    "okuuva/auto-save.nvim", -- maintained fork of Pocco81/auto-save.nvim, which stopped receiving updates in 2024
    config = true, -- unlike Pocco81's fork, this one only turns on once setup() is called
}
